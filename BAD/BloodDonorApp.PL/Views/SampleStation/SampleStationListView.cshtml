@model BloodDonorApp.PL.Models.SampleStationListViewModel

@{
    ViewBag.Title = "View";
    var rowNumber = 1;
    var isAdmin = User.IsInRole("Admin") || User.IsInRole("RootAdmin");
    var visibleFor = isAdmin ? "text-align:center;" : "text-align:center; display:none";
}

<div class="container body-content">
    <h1>All sample stations</h1>
    <br />
    <table class="table" style="font-size:150%">
        <thead>
            <tr>
                <th scope="col">#</th>
                <th scope="col">Name</th>
                <th scope="col">Street</th>
                <th scope="col">City</th>
                <th scope="col" style="@visibleFor">Actions</th>
            </tr>
        </thead>

        @foreach (var item in Model.SampleStations)
        {
            <tr>
                <td scope="row">@(rowNumber++)</td>
                <td>@item.Name</td>
                <td>@item.Street</td>
                <td>@item.City</td>
                <td class="" style="@visibleFor">
                    <button type="button" class="boxed-btn-details" title="Show details for @item.Name" onclick="location.href='@Url.Action("Details", "SampleStation", new { id = item.Id })'">Details</button>
                </td>
            </tr>


        }
    </table>
    <hr />
    <button type="button" class="boxed-btn" style="@visibleFor" title="Add new Hospital" onclick="location.href='@Url.Action("Create", "SampleStation")'">Add new Sample station</button>
</div>
